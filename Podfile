# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShareRide' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ShareRide
  pod 'Firebase/Database'
pod 'Firebase/Core'
pod 'FirebaseUI/Auth'
pod 'Firebase'

  target 'ShareRideTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShareRideUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
